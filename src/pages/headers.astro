---
import Layout from '../layouts/Layout.astro';
import CodeBlock from '../components/CodeBlock.astro';
---

<Layout title="C++ Headers - Learn C++ Programming">
  <main class="px-6 pb-12">
    <div class="max-w-7xl mx-auto">
      <div class="backdrop-blur-glass bg-glass border border-glass-border rounded-xl p-8">
        <h1 class="text-4xl font-bold text-white mb-8">C++ Standard Headers</h1>
        
        <div class="space-y-12">
          <section>
            <h2 class="text-2xl font-bold text-white mb-4">&lt;iostream&gt;</h2>
            <p class="text-gray-300 mb-4">Input/output stream library for console operations.</p>
            <CodeBlock code={`#include <iostream>
using namespace std;

int main() {
    string name;
    cout << "Enter your name: ";
    cin >> name;
    cout << "Hello, " << name << "!\\n";
    return 0;
}`} />
          </section>

          <section>
            <h2 class="text-2xl font-bold text-white mb-4">&lt;vector&gt;</h2>
            <p class="text-gray-300 mb-4">Dynamic array container from the STL.</p>
            <CodeBlock code={`#include <vector>
#include <iostream>

int main() {
    vector<int> numbers = {1, 2, 3, 4, 5};
    
    // Add elements
    numbers.push_back(6);
    
    // Access elements
    cout << "First element: " << numbers[0] << "\\n";
    
    // Iterate over vector
    for(const auto& num : numbers) {
        cout << num << " ";
    }
    return 0;
}`} />
          </section>

          <section>
            <h2 class="text-2xl font-bold text-white mb-4">&lt;string&gt;</h2>
            <p class="text-gray-300 mb-4">String handling library.</p>
            <CodeBlock code={`#include <string>

int main() {
    string str1 = "Hello";
    string str2 = "World";
    
    // String concatenation
    string result = str1 + " " + str2;
    
    // String methods
    cout << "Length: " << result.length() << "\\n";
    cout << "Substring: " << result.substr(0, 5) << "\\n";
    return 0;
}`} />
          </section>
        </div>
      </div>
    </div>
  </main>
</Layout>